# Projects

A branch position. It refines the root with its own partial `_agent/` (only
`now.md`) — fractal composition: a branch may carry a subset of the contract and
inherits the rest from its ancestors.
