# Reference Space

A known-good, minimal ideaspace. It exercises the shape `validateSpace` checks:
a root `_agent/` contract, READMEs along a path, a knowledge Note with valid
frontmatter, a branch with a partial `_agent/` (fractal composition), and an
unknown underscore folder (`_scratch/`) to confirm graceful-ignore.

Validating this directory yields zero `error`-level issues.
