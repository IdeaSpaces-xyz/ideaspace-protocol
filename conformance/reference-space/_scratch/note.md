This is junk inside an unknown underscore folder. An agent that doesn't
understand `_scratch/` gracefully ignores it — it is infrastructure, not
knowledge, and never causes a conformance error. Note there's deliberately no
frontmatter here, and that's fine because this file is skipped entirely.
