This is payload inside an unknown extension. An agent that doesn't
understand `_scratch/` leaves it quietly opaque — it is extension payload, not
knowledge, and its unknown name causes neither a conformance error nor warning. Note there's deliberately no
frontmatter here, and that's fine because this file is skipped entirely.
