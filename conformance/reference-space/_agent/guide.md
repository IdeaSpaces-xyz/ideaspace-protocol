---
name: Guide
summary: How agent and human work in the reference space — read READMEs and _agent/ along the path, treat .md as knowledge.
---
# Guide

- Read `README.md` and `_agent/` along the path before acting at a position.
- Treat `.md` files as knowledge; treat `_agent/` as instruction.
- Keep `_agent/` small — let `.md` files carry the weight of what is known.
- Surface a named-but-absent contract file as a drift signal, not an error.
